# fastapi-articles-service
FastAPI backend service for managing users and articles with role-based access control (RBAC). Includes JWT authentication, PostgreSQL database, Docker setup, database seeding, health check endpoint, and automated API testing with pytest and coverage measurement.
